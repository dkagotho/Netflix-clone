import React, { state } from 'react'

function Row({ title }) {
    const [movies, setMovies] = useState([]);

    

    return (
        <div>
            <h2>{title}</h2>

            {/* container -> posters */}
        </div>
    )
}

export default Row
